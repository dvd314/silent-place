[project]
name = "silent-place"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "msgpack>=1.1.2",
    "websockets>=16.0",
]
